with ortsteile as
(
    select ortsteil,
    to_char(to_timestamp(timestamp/1000), 'yyyy-mm-dd') as Datum,
    count(*) as Anzahl
    from tree_history
    where timestamp > 1641067710000
    group by ortsteil, timestamp
    order by ortsteil, timestamp
)
select ot0.Anzahl as "Althen-Kleinpösna", ot1.Anzahl as "Altlindenau", ot2.Anzahl as "Anger-Crottendorf", ot3.Anzahl as "Baalsdorf", ot4.Anzahl as "Burghausen-Rückmarsdorf", ot5.Anzahl as "Böhlitz-Ehrenberg", ot6.Anzahl as "Connewitz", ot7.Anzahl as "Dölitz-Dösen", ot8.Anzahl as "Engelsdorf", ot9.Anzahl as "Eutritzsch", ot10.Anzahl as "Gohlis-Mitte", ot11.Anzahl as "Gohlis-Nord", ot12.Anzahl as "Gohlis-Süd", ot13.Anzahl as "Großzschocher", ot14.Anzahl as "Grünau-Mitte", ot15.Anzahl as "Grünau-Nord", ot16.Anzahl as "Grünau-Ost", ot17.Anzahl as "Grünau-Siedlung", ot18.Anzahl as "Hartmannsdorf-Knautnaundorf", ot19.Anzahl as "Heiterblick", ot20.Anzahl as "Holzhausen", ot21.Anzahl as "Kleinzschocher", ot22.Anzahl as "Knautkleeberg-Knauthain", ot23.Anzahl as "Lausen-Grünau", ot24.Anzahl as "Leutzsch", ot25.Anzahl as "Liebertwolkwitz", ot26.Anzahl as "Lindenau", ot27.Anzahl as "Lindenthal", ot28.Anzahl as "Lößnig", ot29.Anzahl as "Lützschena-Stahmeln", ot30.Anzahl as "Marienbrunn", ot31.Anzahl as "Meusdorf", ot32.Anzahl as "Miltitz", ot33.Anzahl as "Mockau-Nord", ot34.Anzahl as "Mockau-Süd", ot35.Anzahl as "Möckern", ot36.Anzahl as "Mölkau", ot37.Anzahl as "Neulindenau", ot38.Anzahl as "Neustadt-Neuschönefeld", ot39.Anzahl as "Paunsdorf", ot40.Anzahl as "Plagwitz", ot41.Anzahl as "Plaußig-Portitz", ot42.Anzahl as "Probstheida", ot43.Anzahl as "Reudnitz-Thonberg", ot44.Anzahl as "Schleußig", ot45.Anzahl as "Schönau", ot46.Anzahl as "Schönefeld-Abtnaundorf", ot47.Anzahl as "Schönefeld-Ost", ot48.Anzahl as "Seehausen", ot49.Anzahl as "Sellerhausen-Stünz", ot50.Anzahl as "Stötteritz", ot51.Anzahl as "Südvorstadt", ot52.Anzahl as "Thekla", ot53.Anzahl as "Volkmarsdorf", ot54.Anzahl as "Wahren", ot55.Anzahl as "Wiederitzsch", ot56.Anzahl as "Zentrum", ot57.Anzahl as "Zentrum-Nord", ot58.Anzahl as "Zentrum-Nordwest", ot59.Anzahl as "Zentrum-Ost", ot60.Anzahl as "Zentrum-Süd", ot61.Anzahl as "Zentrum-Südost", ot62.Anzahl as "Zentrum-West" from
(select Datum, Anzahl from ortsteile where ortsteil = 'Althen-Kleinpösna') as ot0 join (select Datum, Anzahl from ortsteile where ortsteil = 'Altlindenau') as ot1 on ot0.Datum = ot1.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Anger-Crottendorf') as ot2 on ot1.Datum = ot2.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Baalsdorf') as ot3 on ot2.Datum = ot3.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Burghausen-Rückmarsdorf') as ot4 on ot3.Datum = ot4.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Böhlitz-Ehrenberg') as ot5 on ot4.Datum = ot5.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Connewitz') as ot6 on ot5.Datum = ot6.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Dölitz-Dösen') as ot7 on ot6.Datum = ot7.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Engelsdorf') as ot8 on ot7.Datum = ot8.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Eutritzsch') as ot9 on ot8.Datum = ot9.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Gohlis-Mitte') as ot10 on ot9.Datum = ot10.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Gohlis-Nord') as ot11 on ot10.Datum = ot11.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Gohlis-Süd') as ot12 on ot11.Datum = ot12.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Großzschocher') as ot13 on ot12.Datum = ot13.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Grünau-Mitte') as ot14 on ot13.Datum = ot14.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Grünau-Nord') as ot15 on ot14.Datum = ot15.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Grünau-Ost') as ot16 on ot15.Datum = ot16.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Grünau-Siedlung') as ot17 on ot16.Datum = ot17.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Hartmannsdorf-Knautnaundorf') as ot18 on ot17.Datum = ot18.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Heiterblick') as ot19 on ot18.Datum = ot19.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Holzhausen') as ot20 on ot19.Datum = ot20.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Kleinzschocher') as ot21 on ot20.Datum = ot21.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Knautkleeberg-Knauthain') as ot22 on ot21.Datum = ot22.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Lausen-Grünau') as ot23 on ot22.Datum = ot23.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Leutzsch') as ot24 on ot23.Datum = ot24.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Liebertwolkwitz') as ot25 on ot24.Datum = ot25.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Lindenau') as ot26 on ot25.Datum = ot26.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Lindenthal') as ot27 on ot26.Datum = ot27.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Lößnig') as ot28 on ot27.Datum = ot28.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Lützschena-Stahmeln') as ot29 on ot28.Datum = ot29.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Marienbrunn') as ot30 on ot29.Datum = ot30.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Meusdorf') as ot31 on ot30.Datum = ot31.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Miltitz') as ot32 on ot31.Datum = ot32.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Mockau-Nord') as ot33 on ot32.Datum = ot33.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Mockau-Süd') as ot34 on ot33.Datum = ot34.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Möckern') as ot35 on ot34.Datum = ot35.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Mölkau') as ot36 on ot35.Datum = ot36.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Neulindenau') as ot37 on ot36.Datum = ot37.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Neustadt-Neuschönefeld') as ot38 on ot37.Datum = ot38.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Paunsdorf') as ot39 on ot38.Datum = ot39.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Plagwitz') as ot40 on ot39.Datum = ot40.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Plaußig-Portitz') as ot41 on ot40.Datum = ot41.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Probstheida') as ot42 on ot41.Datum = ot42.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Reudnitz-Thonberg') as ot43 on ot42.Datum = ot43.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Schleußig') as ot44 on ot43.Datum = ot44.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Schönau') as ot45 on ot44.Datum = ot45.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Schönefeld-Abtnaundorf') as ot46 on ot45.Datum = ot46.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Schönefeld-Ost') as ot47 on ot46.Datum = ot47.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Seehausen') as ot48 on ot47.Datum = ot48.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Sellerhausen-Stünz') as ot49 on ot48.Datum = ot49.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Stötteritz') as ot50 on ot49.Datum = ot50.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Südvorstadt') as ot51 on ot50.Datum = ot51.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Thekla') as ot52 on ot51.Datum = ot52.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Volkmarsdorf') as ot53 on ot52.Datum = ot53.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Wahren') as ot54 on ot53.Datum = ot54.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Wiederitzsch') as ot55 on ot54.Datum = ot55.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Zentrum') as ot56 on ot55.Datum = ot56.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Zentrum-Nord') as ot57 on ot56.Datum = ot57.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Zentrum-Nordwest') as ot58 on ot57.Datum = ot58.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Zentrum-Ost') as ot59 on ot58.Datum = ot59.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Zentrum-Süd') as ot60 on ot59.Datum = ot60.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Zentrum-Südost') as ot61 on ot60.Datum = ot61.Datum  join (select Datum, Anzahl from ortsteile where ortsteil = 'Zentrum-West') as ot62 on ot61.Datum = ot62.Datum